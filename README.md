# homebrew-tap

Homebrew tap for [BurnRate](https://github.com/burnrate-dev/burnrate) CLI.

## Install

```bash
brew tap burnrate-dev/tap
brew install burnrate
```

Or in one command:

```bash
brew install burnrate-dev/tap/burnrate
```
